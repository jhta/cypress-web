// to suport IE 11
import 'core-js/stable'

import EmailsInput from './lib'

// it is load as global var (ough)
global.EmailsInput = EmailsInput

// or it is exported as module
export default EmailsInput
